# JAVA - Bucles

# 🌀 **1. ¿Qué es un Bucle en Java?**

Un **bucle** (o *loop*) es una estructura que permite **repetir un bloque de código múltiples veces**, mientras se cumpla una condición.

Los loops son fundamentales porque permiten:

- Automatizar tareas repetitivas
- Procesar datos de manera eficiente
- Simular ciclos, turnos, procesos o iteraciones
- Construir juegos, menús, validaciones y más

**Sin loops** tendríamos que copiar y pegar el mismo código muchas veces.

---

# 🎯 **2. Tipos de Bucles en Java**

Java incluye cuatro tipos principales de bucles:

| Tipo de Bucle | Cuándo se usa | Característica principal |
| --- | --- | --- |
| **while** | Cuando no sabes cuántas veces se repetirá | Repite mientras una condición sea verdadera |
| **do-while** | Cuando debe ejecutarse *al menos una vez* | Evalúa la condición después |
| **for** | Cuando sabes cuántas iteraciones necesitas | Control total del contador |
| **for-each** | Cuando recorres arreglos o colecciones | Más legible y seguro |

*(For-each se enseña formalmente cuando lleguemos a arrays, pero lo incluimos para el módulo de loops.)*

---

# 🧩 **3. Bucle WHILE**

El **while** repite un bloque siempre que la condición sea verdadera.

## 📘 Sintaxis:

```java
while (condicion) {
    // código a repetir
}
```

## ✔ Ejemplo básico:

```java
int contador = 1;

while (contador <= 5) {
    System.out.println("Iteración " + contador);
    contador++; // evita loop infinito
}
```

---

# 🧠 ¿Cuándo usar while?

- Si NO sabemos cuántas veces se repetirá.
- Cuando la repetición depende del usuario o de la lógica del programa.
- En validaciones, menús, intentos o simulaciones.

---

# ⚠️ Errores comunes con while

❌ Loop infinito:

```java
while (i < 5) {
    System.out.println(i);
}
// i nunca cambia
```

---

# 🧩 **4. Bucle DO-WHILE**

Se ejecuta **al menos una vez**, porque la condición se evalúa al final.

## 📘 Sintaxis:

```java
do {
    // código a ejecutar
} while (condicion);
```

## ✔ Ejemplo:

```java
int numero = 0;

do {
    System.out.println("Esto se ejecuta una vez como mínimo");
    numero++;
} while (numero < 0);
```

Aunque la condición es falsa, el bloque se ejecuta una vez.

---

# 🧠 ¿Cuándo usar do-while?

- Menús interactivos
- Confirmaciones
- Situaciones donde el usuario debe ver algo primero
- Procesos que siempre deben correr al menos una vez

---

# 🧩 **5. Bucle FOR**

Ideal cuando conoces la cantidad de repeticiones.

## 📘 Sintaxis:

```java
for (inicial; condicion; incremento) {
    // código repetido
}
```

## ✔ Ejemplo:

```java
for (int i = 1; i <= 10; i++) {
    System.out.println("Número: " + i);
}
```

---

# 🧠 ¿Cuándo usar for?

- Contadores
- Rango de números
- Simulaciones con pasos fijos
- Procesos con iteración conocida (ej: 10 turnos, 5 días, 3 intentos)

---

# 🧩 **6. Bucle FOR-EACH**

*(Relacionado a arreglos y colecciones, pero útil en loops)*

Sirve para recorrer arreglos de forma más simple y segura.

## ✔ Ejemplo:

```java
String[] frutas = {"Manzana", "Pera", "Naranja"};

for (String f : frutas) {
    System.out.println(f);
}
```

---

# 🔥 **7. Comparación de Bucles**

| Bucle | Mejor uso | ¿Evalúa antes o después? |
| --- | --- | --- |
| **while** | Repetición desconocida | Antes |
| **do-while** | Ejecutar al menos una vez | Después |
| **for** | Repetición exacta | Antes |
| **foreach** | Recorrer elementos | Antes |

---

# 🧼 **8. Buenas Prácticas al Usar Bucles**

### ✔ Claridad en la condición

Evita condiciones demasiado largas o poco claras.

### ✔ Incremento dentro del loop

Evita loops infinitos.

### ✔ Nombres descriptivos para contadores cuando sea necesario

❌ `int x = 0;`

✔ `int turno = 0;`

### ✔ Evitar modificar el contador dentro del cuerpo del loop (excepto cuando es intencional)

### ✔ Evitar múltiples responsabilidades dentro del loop

Cada loop debe hacer *una cosa*.

### ✔ Revisar límites cuidadosamente

Errores comunes: `<=`, `<`, `>=`, `>`.

---

# 🚫 **9. Malas Prácticas Comunes**

❌ Usar un while para un número fijo de repeticiones.

❌ No actualizar la variable de control.

❌ Anidar demasiados loops sin necesidad.

❌ Usar `break` sin justificación clara.

❌ Usar nombres como `i`, `j`, `k` en loops no triviales.

---

# 🎮 **10. Ejemplos Reales y Claros**

## 🟦 Ejemplo 1 — Contador simple

```java
for (int i = 1; i <= 5; i++) {
    System.out.println("Vueltas: " + i);
}
```

---

## 🟩 Ejemplo 2 — Simulación de vida

```java
int vida = 50;

while (vida > 0) {
    System.out.println("Vida actual: " + vida);
    vida -= 10;
}

System.out.println("Personaje derrotado.");
```

---

## 🟧 Ejemplo 3 — Menú con do-while

```java
int opcion;

do {
    System.out.println("1. Jugar");
    System.out.println("2. Salir");
    opcion = scanner.nextInt();
} while (opcion != 2);
```

---

## 🟥 Ejemplo 4 — Tabla de multiplicar

```java
int n = 7;

for (int i = 1; i <= 10; i++) {
    System.out.println(n + " x " + i + " = " + (n * i));
}
```

---

# 🧪 **11. Ejercicio (Opcional)**

**Crear un programa que:**

1. Use un `for` para imprimir del 1 al 20
2. Use un `while` para hacer una cuenta regresiva de 10 a 1
3. Use un `do-while` para mostrar un menú simple
4. Use `foreach` para recorrer un arreglo de nombres

---

# 🎯 **12. Desafío (Opcional)**

Simular un personaje que avanza niveles:

- Nivel va de 1 a 10 → usar `for`
- Vida se regenera hasta llegar a 100 → usar `while`
- Cada nivel debe mostrar un mensaje especial al menos una vez → usar `do-while`
- Al final mostrar una lista de logros usando `foreach`

# Bucle en java

```java
import java.util.Scanner;

public class Bucles {
    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        int opcion;
        boolean juegoActivo = true; //  flag principal

        // Esto es previo, aun no lo veremos pero es necesario para el ejemplo "un Mini Arreglo"
        String[] recompensas = {
                "Espada de Madera",
                "Escudo Básico",
                "Casco de Hierro",
                "Poción de Energia"
        };

        // ==========
        // DO - WHILE
        // ==========

        // Generalmente usado en menus

        do {
            System.out.println("SISTEMA DE ENTRENAMIENTO");
            System.out.println("===== JAVALIMOS ========");
            System.out.println("1. Iniciar entrenamiento");
            System.out.println("2. Ver recompensas");
            System.out.println("3. Salir");
            System.out.print("Seleccione una opción: ");

            opcion = scanner.nextInt();

            switch (opcion){
                case 1 -> {
                    System.out.println("\nEntrenamiento iniciado....");

                    int energia = 100;
                    boolean personajePuedeContinuar = true; // Flag de control para el entrenamiento

                    //FOR : niveles del 1 al 5
                    // Primera columna de la izquierda, nos sirve para inicializar variable de inicio
                    // Columna central es nuestra condicion, hasta cuando va recorrer
                    // Tercera columna es nuestro contador aumentando de 1 en 1 por cada iteracion
                    for ( int nivel = 1; nivel <=5 ; nivel++){

                        if(!personajePuedeContinuar){
                            System.out.println("El personaje ya no puede continuar el entreamiento");
                            break;
                        }

                        System.out.println(" ---- Nivel ----" + nivel);
                        System.out.println("Energia inicial : " + energia);

                        //While : Para bajar la energia mientras todavia sea suficiente ante de llegar a 0
                        while ( energia > 0 && energia > 20){
                            energia -=15;
                            System.out.println("Entremiento... energia actual " + energia);
                        }

                        if (energia <= 20){
                            personajePuedeContinuar = false;
                            System.out.println("La energia es demaciado baja. Entrenamiento finalizado");
                        }else{
                            System.out.println("Nivel de entrenamiento completado con éxito");
                        }

                    }
                    
                }
                case 2 ->{
                    System.out.println(" RECOMPENSAS DISPONIBLES");

                    // EL FOR EACH : recorrer  el arreglo "recompensas" y va hacer es que busca cada elemento dentro de el
                    /*
                          String[] recompensas = {
                            "Espada de Madera",
                            "Escudo Básico",
                            "Casco de Hierro",
                            "Poción de Energia"
                    };
                     */
                    for(String nuevaVariableRecomensa: recompensas){
                        System.out.println(" - " + nuevaVariableRecomensa);
                    }
                    System.out.println();
                }

                case 3 -> {
                    juegoActivo = false;
                    System.out.println("\nSaliendo del programa");
                }

                default -> System.out.println("Opcion invalida: Intente nuevamente\n");
            }



        }while (juegoActivo);

        System.out.println("Programa Finalizado");
        scanner.close();

    }
}

```

---

>*fuente: https://righteous-baron-17e.notion.site/JAVA-Bucles-3354db47a25580ababe2ecbdeda10ea2*
