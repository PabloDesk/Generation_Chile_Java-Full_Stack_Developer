# 🧪 Actividad Guiada: Menú de Variables en Java

## 🎯 Objetivo

Comprender cómo funcionan las **variables en Java**, los **tipos primitivos** y sus **Wrapper Classes**, utilizando un menú interactivo en consola.

---

# 🧠 1. ¿Qué es un `print` en Java?

Un `print` permite **mostrar información en pantalla (consola)**.

## 📌 Tipos principales:

```java
System.out.print("Hola");
```

➡ Imprime sin salto de línea

```java
System.out.println("Hola");
```

➡ Imprime con salto de línea

```java
System.out.printf("Hola %s","mundo");
```

➡ Imprime con formato (similar a C)

---

# 🚀 2. Actividad: Crear un Menú de Variables

## 🧩 Paso 1: Estructura base

```java
import java.util.Scanner;

public class MenuVariables {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int opcion;

        do {
            System.out.println("\n=== MENÚ DE VARIABLES ===");
            System.out.println("1. Tipos Primitivos");
            System.out.println("2. Wrapper Classes");
            System.out.println("3. Salir");
            System.out.print("Seleccione una opción: ");

            opcion = scanner.nextInt();

            switch (opcion) {
                case 1:
                    mostrarPrimitivos();
                    break;
                case 2:
                    mostrarWrappers();
                    break;
                case 3:
                    System.out.println("Saliendo...");
                    break;
                default:
                    System.out.println("Opción inválida");
            }

        } while (opcion != 3);

        scanner.close();
    }
```

---

## 🧩 Paso 2: Tipos Primitivos

```java
    public static void mostrarPrimitivos() {
        System.out.println("\n--- TIPOS PRIMITIVOS ---");

        byte b = 127;
        short s = 32000;
        int i = 100000;
        long l = 10000000000L;

        float f = 3.14f;
        double d = 3.1415926535;

        char c = 'A';
        boolean bool = true;

        System.out.println("byte: " + b);
        System.out.println("short: " + s);
        System.out.println("int: " + i);
        System.out.println("long: " + l);

        System.out.println("float: " + f);
        System.out.println("double: " + d);

        System.out.println("char: " + c);
        System.out.println("boolean: " + bool);

        // Explicación
        System.out.println("\nExplicación:");
        System.out.println("byte, short, int, long → números enteros");
        System.out.println("float, double → números decimales");
        System.out.println("char → un solo carácter");
        System.out.println("boolean → true o false");
    }
```

---

## 🧩 Paso 3: Wrapper Classes

```java
    public static void mostrarWrappers() {
        System.out.println("\n--- WRAPPER CLASSES ---");

        Byte b = 127;
        Short s = 32000;
        Integer i = 100000;
        Long l = 10000000000L;

        Float f = 3.14f;
        Double d = 3.1415926535;

        Character c = 'A';
        Boolean bool = true;

        System.out.println("Byte: " + b);
        System.out.println("Short: " + s);
        System.out.println("Integer: " + i);
        System.out.println("Long: " + l);

        System.out.println("Float: " + f);
        System.out.println("Double: " + d);

        System.out.println("Character: " + c);
        System.out.println("Boolean: " + bool);

        // Explicación
        System.out.println("\nExplicación:");
        System.out.println("Los Wrapper son versiones OBJETO de los tipos primitivos");
        System.out.println("Permiten usar métodos y trabajar con colecciones");
        System.out.println("Ejemplo: Integer.parseInt(\"123\")");
    }
}
```

---

# 🧪 3. ¿Qué deberían ver los estudiantes?

Cuando ejecuten el programa:

```
=== MENÚ DE VARIABLES ===
1. Tipos Primitivos
2. Wrapper Classes
3. Salir
Seleccione una opción:
```

Y al elegir opciones:

👉 Verán valores reales en consola

👉 Entenderán tipos de datos **viendo resultados reales**

👉 Relacionarán teoría con ejecución

---

# 💡 4. Concepto clave (para reforzar en clase)

| Tipo | Qué es | Ejemplo |
| --- | --- | --- |
| Primitivo | Valor simple | `int x = 10;` |
| Wrapper | Objeto | `Integer x = 10;` |

---

# 🎯 5. Mini desafío (opcional)

Agregar una nueva opción:

```
4. Ingresar datos por teclado
```

👉 Que el usuario ingrese:

- un número entero
- un decimal
- un texto

Y mostrarlo en consola.

---

# 🧠 Resultado

Con esta actividad logran:

- Ver variables **en acción**
- Entender diferencia entre **primitivo vs objeto**
- Practicar **menús + switch + Scanner**
- Relacionar salida (`print`) con lógica

---

https://righteous-baron-17e.notion.site/Actividad-Guiada-Men-de-Variables-en-Java-3344db47a255804e8565d518b5441b5f
