# 🐾 **Refugio Animal** Avanzado

---

# 🐾 **Refugio Animal – Sistema de Gestión Completo**

Este proyecto integra **Programación Orientada a Objetos (POO)** con **estructuras de control**, **colecciones**, **herencia**, **polimorfismo**, **abstracción**, **interfaces**, **menús**, **validaciones**, y **manejo de errores**, simulando un sistema real de gestión de un refugio de animales.

<aside>

NO ESTA PROBADO ASI QUE PUEDE TENER FALLAS

</aside>

---

## 🎯 Objetivo General

Construir un sistema por consola que permita:

- Registrar **clientes**
- Registrar **mascotas**
- Asociar mascotas a clientes
- Aplicar **herencia, polimorfismo, abstracción e interfaces**
- Gestionar datos usando **colecciones**
- Navegar el sistema mediante **menús**
- Validar entradas y manejar errores
- Comprender cómo Java maneja la memoria (GC)

---

## 🧠 Contenidos Integrados

✔ Variables y tipos

✔ Condicionales (`if`, `switch`)

✔ Bucles (`while`, `for`)

✔ Arrays y Colecciones (`List`, `Map`)

✔ Métodos y constructores

✔ Encapsulación

✔ Herencia

✔ Polimorfismo (dinámico y estático)

✔ Abstracción

✔ Interfaces

✔ Paquetes

✔ Scanner + validaciones

✔ Manejo de excepciones (`try / catch`)

✔ Uso correcto de referencias (GC friendly)

---

## 📁 Estructura del Proyecto

```
src/
├── app/
│   └── Main.java
├── model/
│   ├── Persona.java
│   ├── Cliente.java
│   ├── Mascota.java
│   ├── Perro.java
│   ├── Gato.java
├── interfaces/
│   ├── Adoptable.java
│   ├── Entrenable.java
├── service/
│   └── RefugioService.java
├── util/
│   └── InputUtil.java

```

---

## 🧩 Modelo del Dominio

### 👤 Personas (Abstracción + Herencia)

```java
public abstract class Persona {
    protected String nombre;

    public Persona(String nombre) {
        this.nombre = nombre;
    }

    public String getNombre() {
        return nombre;
    }
}

```

```java
public class Cliente extends Persona {
    private List<Mascota> mascotas = new ArrayList<>();

    public Cliente(String nombre) {
        super(nombre);
    }

    public void agregarMascota(Mascota m) {
        mascotas.add(m);
    }

    public List<Mascota> getMascotas() {
        return mascotas;
    }
}

```

---

### 🐾 Mascotas (Abstracción + Polimorfismo)

```java
public abstract class Mascota {
    protected String nombre;
    protected int edad;

    public Mascota(String nombre, int edad) {
        this.nombre = nombre;
        this.edad = edad;
    }

    public abstract void hacerSonido();

    public void mostrarInfo() {
        System.out.println(nombre + " | Edad: " + edad);
    }
}

```

```java
public class Perro extends Mascota implements Adoptable, Entrenable {

    public Perro(String nombre, int edad) {
        super(nombre, edad);
    }

    @Override
    public void hacerSonido() {
        System.out.println(nombre + " dice: Guau 🐶");
    }

    @Override
    public String datosAdopcion() {
        return "Perro " + nombre + " listo para adopción";
    }

    @Override
    public void entrenar() {
        System.out.println(nombre + " ha sido entrenado 🐾");
    }
}

```

```java
public class Gato extends Mascota implements Adoptable {

    public Gato(String nombre, int edad) {
        super(nombre, edad);
    }

    @Override
    public void hacerSonido() {
        System.out.println(nombre + " dice: Miau 🐱");
    }

    @Override
    public String datosAdopcion() {
        return "Gato " + nombre + " disponible para adopción";
    }
}

```

---

## 🔌 Interfaces (Contratos)

```java
public interface Adoptable {
    String datosAdopcion();
}

```

```java
public interface Entrenable {
    void entrenar();
}

```

---

## 🧠 Servicio Principal (Lógica del Sistema)

```java
public class RefugioService {

    private List<Cliente> clientes = new ArrayList<>();
    private Scanner sc = new Scanner(System.in);

    public void iniciar() {
        boolean salir = false;

        while (!salir) {
            System.out.println("\n=== REFUGIO ANIMAL ===");
            System.out.println("1) Crear cliente");
            System.out.println("2) Registrar mascota");
            System.out.println("3) Ver clientes y mascotas");
            System.out.println("0) Salir");

            int op = InputUtil.leerInt();

            switch (op) {
                case 1 -> crearCliente();
                case 2 -> registrarMascota();
                case 3 -> mostrarDatos();
                case 0 -> salir = true;
                default -> System.out.println("Opción inválida");
            }
        }
    }
}

```

---

## 🔁 Uso de Polimorfismo

```java
List<Mascota> mascotas = cliente.getMascotas();

for (Mascota m : mascotas) {
    m.mostrarInfo();
    m.hacerSonido();
}

```

👉 **No importa si es `Perro` o `Gato`**, Java decide qué método ejecutar.

---

## ⚠️ Manejo de errores (try/catch)

```java
try {
    int edad = Integer.parseInt(input);
} catch (NumberFormatException e) {
    System.out.println("Edad inválida");
}

```

---

## ♻️ Memoria y Garbage Collector (conceptual)

- Cuando un objeto deja de tener referencias → **GC lo elimina**
- Al eliminar mascotas o clientes de una lista, Java libera memoria automáticamente
- No se usa `delete` ni `free`

---

## 🧪 Actividades sugeridas (para los estudiantes)

### Nivel 1

- Agregar validaciones (edad > 0)
- No permitir mascotas duplicadas

### Nivel 2

- Usar `Map<Cliente, List<Mascota>>`
- Filtrar mascotas adoptables

### Nivel 3 (Pro)

- Contador de adopciones
- Reportes por tipo de mascota
- Menú exclusivo para adopción

---

## 🔮 Proyección futura (NO implementar aún)

Este mismo proyecto se puede extender a:

- `abstract class Mascota` + `calcularCosto()`
- Interfaces `Vacunable`, `Castrable`
- Persistencia con archivos
- Spring Boot + REST
- Base de datos

---

## ✅ Criterios de Evaluación

✔ Uso correcto de POO

✔ Código ordenado y legible

✔ Uso de colecciones

✔ Aplicación de herencia y polimorfismo

✔ Manejo de errores

✔ Diseño extensible

---

## 🧠 Conclusión

Este proyecto **no es un ejercicio**, es una **base de sistema real**, diseñada para:

- Consolidar POO
- Preparar al estudiante para frameworks
- Entender arquitectura básica
- Pensar en diseño, no solo en código

---

https://righteous-baron-17e.notion.site/Refugio-Animal-Avanzado-33a4db47a255805a9c69fa4c6e0d2d03
